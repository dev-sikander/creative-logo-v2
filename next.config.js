/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.logodesignscenter.com',
            'http://localhost:3000/',
        ],
    },
}
module.exports = nextConfig